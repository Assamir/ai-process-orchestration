# AGENTS.md — Agent operating guidelines

> Generated by claude-agent-scaffold (phase 1, static). `{{PLACEHOLDER}}` markers are
> filled in by the phase-2 Claude Code skill `{{SKILL_NAME}}`. Keep this file focused
> and actionable — record only what an agent cannot infer from the code itself.

- **Project language:** {{PROJECT_LANGUAGE}}
- **Build tool:** {{BUILD_TOOL}}
- **Generated:** {{GENERATED_AT}}

## Identity

Every agent in `/.ai/agents/` is a focused specialist with a single business
responsibility. An agent must know its scope, its boundaries, and which other
agents it can hand work to.

- Mission of this agent system: {{PROJECT_MISSION}}
- Autonomy level and escalation rules: {{AUTONOMY_RULES}}

## Protocol

- Stay within your declared responsibility; hand off anything outside it.
- Treat `/.ai/guidelines/` as the source of truth for how code must be written.
- Never invent directory layout or conventions — read them here first.
- Context & memory rules: {{CONTEXT_RULES}}

## Tools

Declare the external tools and APIs each agent may use in that agent's own file.
Keep toolsets minimal — list only what the responsibility actually requires.

- Shared tools available to all agents: {{SHARED_TOOLS}}

## Quality (non-negotiable)

> **Iron QA rule:** All code produced by any agent MUST ship with tests written in
> **{{TEST_FRAMEWORK}}**. New behavior requires new tests; changed behavior requires
> updated tests. Code without passing tests is not done.

- Tests assert intended behavior, not a mirror of the implementation.
- Cover edge cases (null/empty/error paths), not just the happy path.
- Coding standards: see [guidelines/coding-standards.md](guidelines/coding-standards.md).
- Naming conventions: see [guidelines/naming-conventions.md](guidelines/naming-conventions.md).
- Additional quality gates: {{QUALITY_GATES}}
