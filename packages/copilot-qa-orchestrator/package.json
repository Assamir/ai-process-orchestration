{
  "name": "copilot-qa-orchestrator",
  "version": "0.37.0",
  "description": "Scaffold an AI-driven QA-process orchestration (test planning, ticket review, case design, automation, RCA, test data) into your repo for GitHub Copilot in VS Code. Phase 1 is a deterministic npx installer; phase 2 runs in Copilot.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "copilot-qa-orchestrator": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0"
  },
  "devDependencies": {
    "@qa-orch/core": "*",
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
