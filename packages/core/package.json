{
  "name": "@qa-orch/core",
  "version": "0.4.0",
  "private": true,
  "description": "Shared core for the QA-process orchestration scaffolders: stack detection, wizard, render engine, the logical QA skill model, and the platform-adapter contract. Not published — bundled into each leaf package.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.7.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^1.4.0"
  }
}
